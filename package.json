{
  "name": "readback",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "npm test -w @readback/core && npm run vectors -w @readback/core",
    "dev": "npm run dev -w @readback/web",
    "build": "npm run build -w @readback/web"
  }
}
